/**
 * Один источник и для дева, и для собранной версии: дев-сервер проксирует
 * `/api` на бэкенд, nginx делает то же самое. Переменная нужна лишь тогда,
 * когда API живёт на другом домене.
 */
export const API_URL: string = import.meta.env.VITE_API_URL ?? '/api';
