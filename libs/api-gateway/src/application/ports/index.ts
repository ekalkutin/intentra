/*
 * Клиентские порты живут в `libs/api-ports`, а не здесь: их связывает
 * композиционный корень, и знать о них должен не только шлюз — со временем
 * появится второй фасад. Здесь только реэкспорт, чтобы внутренности шлюза
 * ходили одним путём.
 */
export { IamClientPort, WorkspaceClientPort } from '@intentra/api-ports';
