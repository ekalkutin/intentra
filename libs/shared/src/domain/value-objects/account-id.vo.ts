import { EntityId } from './entity-id.vo.js';

/**
 * Человек как субъект платформы.
 *
 * Живёт в общем ядре, потому что на него ссылается каждый контекст: IAM им
 * владеет, остальные хранят только идентификатор. Внешних ключей между ними
 * нет и быть не может — у каждого своя база (ADR 0001), — поэтому единственное
 * общее, что у них есть про человека, это форма его идентификатора.
 */
export class AccountId extends EntityId {
  declare private readonly __type: 'AccountId';
}
