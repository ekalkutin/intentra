/*
 * Клиентские порты живут в `libs/api-ports`, а не здесь: их связывает
 * композиционный корень, и знать о них должен не только фасад. Здесь только
 * реэкспорт, чтобы внутренности шлюза ходили одним путём — каким бы
 * транспортом ни пришёл запрос.
 */
export { IamClientPort, WorkspaceClientPort } from '@intentra/api-ports';
