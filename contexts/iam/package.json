{
  "name": "@intentra/iam",
  "version": "0.0.0",
  "private": true,
  "description": "Identity & Account Management: учётные записи и вход в систему",
  "type": "module",
  "exports": {
    ".": {
      "types": "./build/index.d.ts",
      "import": "./build/index.js"
    }
  },
  "files": [
    "build"
  ],
  "scripts": {
    "db:generate": "prisma format && prisma generate",
    "db:migrate": "prisma migrate dev",
    "build": "tsc -p tsconfig.build.json",
    "watch": "tsc -p tsconfig.build.json --watch --preserveWatchOutput",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "nx": {
    "tags": [
      "type:context",
      "deployment:monolith",
      "deployment:microservices"
    ]
  },
  "dependencies": {
    "@intentra/iam-contracts": "workspace:*",
    "@intentra/shared": "workspace:*",
    "@nestjs/common": "catalog:nestjs12",
    "@nestjs/core": "catalog:nestjs12",
    "@nestjs/cqrs": "catalog:nestjs12",
    "@nestjs/jwt": "catalog:nestjs12",
    "@prisma/adapter-pg": "^7.9.1",
    "@prisma/client": "^7.9.1",
    "dotenv": "catalog:",
    "reflect-metadata": "^0.2.2",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@nestjs/testing": "catalog:nestjs12",
    "@types/node": "catalog:tooling",
    "prisma": "^7.9.1",
    "vitest": "catalog:tooling"
  }
}
